# Point these at whichever host/port each service is published on.
# All three keys fall back to localhost defaults in src/api/client.ts, so this
# file is optional for local development.
VITE_DOCUMENT_SERVICE_URL=http://localhost:8000
VITE_QUERY_SERVICE_URL=http://localhost:8003
VITE_RETRIEVAL_SERVICE_URL=http://localhost:8002
